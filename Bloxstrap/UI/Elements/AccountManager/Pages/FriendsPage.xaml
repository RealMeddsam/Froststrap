<ui:UiPage x:Class="Bloxstrap.UI.Elements.AccountManagers.Pages.FriendsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:dmodels="clr-namespace:Bloxstrap.UI.ViewModels.AccountManagers"
      xmlns:controls="clr-namespace:Bloxstrap.UI.Elements.Controls"
      xmlns:enums="clr-namespace:Bloxstrap.Enums"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:resources="clr-namespace:Bloxstrap.Resources"
      mc:Ignorable="d"
      d:DesignHeight="500" d:DesignWidth="850"
      Title="Friends Page"
      Scrollable="True">

    <Grid>
        <!-- Lowkey since we doing everything in its own tab, we can add here more stuff like bulk unfriend etc etc -->
        <Border Padding="12" Background="{DynamicResource ControlFillColorDefaultBrush}" CornerRadius="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock VerticalAlignment="Center" FontSize="20" FontWeight="Bold" Text="Friends" />
                </StackPanel>

                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
                    <TextBlock VerticalAlignment="Center" Text="Filter:" Margin="0,0,8,0" />
                    <ComboBox Width="100" ItemsSource="{Binding FriendFilters}" SelectedItem="{Binding SelectedFriendFilter}" />
                    <ui:Button Margin="8,0,0,0" Command="{Binding RefreshFriendsCommand}" Content="Refresh" />
                </StackPanel>

                <ItemsControl ItemsSource="{Binding FilteredFriends}" Grid.Row="2" HorizontalAlignment="Stretch">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid HorizontalAlignment="Stretch" VerticalAlignment="Top" Columns="6"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border
                                    Margin="4"
                                    Padding="8"
                                    Width="120"
                                    Background="{DynamicResource ControlFillColorDefaultBrush}"
                                    CornerRadius="6">
                                <StackPanel>
                                    <Grid>
                                        <Border
                                                Width="64"
                                                Height="64"
                                                Background="{DynamicResource ControlFillColorSecondaryBrush}"
                                                CornerRadius="32">
                                            <Image 
                                                    ClipToBounds="True" 
                                                    Source="{Binding AvatarUrl, TargetNullValue={x:Null}, FallbackValue={x:Null}}" 
                                                    Stretch="UniformToFill" />
                                        </Border>
                                        <Border
                                                Width="16"
                                                Height="16"
                                                Margin="0,0,16,0"
                                                HorizontalAlignment="Right"
                                                VerticalAlignment="Bottom"
                                                Background="{Binding StatusColor}"
                                                BorderBrush="{DynamicResource ApplicationBackgroundBrush}"
                                                BorderThickness="2"
                                                CornerRadius="8"
                                                ToolTip="{Binding LastLocation}">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="4" Opacity="0.6" ShadowDepth="0" />
                                            </Border.Effect>
                                        </Border>
                                    </Grid>

                                    <TextBlock
                                            Margin="0,6,0,0"
                                            FontSize="12"
                                            FontWeight="SemiBold"
                                            Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                            Text="{Binding DisplayName}"
                                            TextTrimming="CharacterEllipsis" />

                                    <TextBlock
                                            Margin="0,2,0,0"
                                            FontSize="10"
                                            Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                            Text="{Binding LastLocation}"
                                            ToolTip="{Binding LastLocation}"
                                            TextTrimming="CharacterEllipsis" />

                                    <ui:Button
                                            Margin="0,8,0,0"
                                            HorizontalAlignment="Stretch"
                                            Appearance="Primary"
                                            Command="{Binding DataContext.JoinFriendCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                            CommandParameter="{Binding}"
                                            Height="30"
                                            IsEnabled="{Binding IsOnline}">
                                        <ui:SymbolIcon Filled="True" Symbol="Play24" FontSize="16" />
                                    </ui:Button>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Grid>
        </Border>

        <Grid Panel.ZIndex="999" Visibility="{Binding IsPresenceLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <Border Background="#A0000000" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border Background="{DynamicResource NormalDarkAndLightBackground}"
                        CornerRadius="12"
                        Padding="20"
                        Effect="{DynamicResource ControlShadow}">
                    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Orientation="Vertical">
                        <TextBlock Text="Loading friends list" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,12"
                                   Foreground="{DynamicResource TextFillColorPrimaryBrush}" HorizontalAlignment="Center"/>
                        <ui:ProgressRing IsIndeterminate="True" Width="56" Height="56" HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</ui:UiPage>