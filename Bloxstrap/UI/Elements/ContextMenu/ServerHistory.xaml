<base:WpfUiWindow x:Class="Bloxstrap.UI.Elements.ContextMenu.ServerHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bloxstrap.UI.Elements.ContextMenu"
        xmlns:base="clr-namespace:Bloxstrap.UI.Elements.Base"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        xmlns:models="clr-namespace:Bloxstrap.UI.ViewModels.ContextMenu"
        xmlns:resources="clr-namespace:Bloxstrap.Resources"
        xmlns:enums="clr-namespace:Bloxstrap.Enums"
        d:DataContext="{d:DesignInstance Type=models:ServerHistoryViewModel}"
        mc:Ignorable="d"
        Title="{x:Static resources:Strings.ContextMenu_GameHistory_Title}"
        MinWidth="520"
        MinHeight="520"
        Width="680"
        Height="520"
        Background="{ui:ThemeResource ApplicationBackgroundBrush}"
        ExtendsContentIntoTitleBar="True"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.Background>
            <DynamicResource ResourceKey="WindowBackgroundGradient" />
        </Grid.Background>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid x:Name="ImageBackgroundLayer" Panel.ZIndex="-1" Visibility="{Binding IsImageMode, Converter={StaticResource BooleanToVisibilityConverter}}" Grid.RowSpan="5">
            <Border Background="{DynamicResource WindowBackgroundImage}" />
            <Border Background="{DynamicResource WindowBackgroundBlackOverlay}" />
        </Grid>

        <ui:TitleBar Grid.Row="0" Padding="8" x:Name="RootTitleBar" Title="{x:Static resources:Strings.ContextMenu_GameHistory_Title}" ShowMinimize="False" ShowMaximize="False" CanMaximize="False" KeyboardNavigation.TabNavigation="None" Icon="pack://application:,,,/Bloxstrap.ico" />

        <TextBlock Grid.Row="1" Margin="16,8,16,0" Text="{x:Static resources:Strings.ContextMenu_GameHistory_Description}" TextWrapping="Wrap" />

        <!-- Controls Section -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="16,8,16,0">
            <ComboBox x:Name="SortComboBox" 
                     Width="150" 
                     ItemsSource="{Binding SortOptions}"
                     DisplayMemberPath="Key"
                     SelectedValuePath="Value"
                     SelectedValue="{Binding SelectedSortOption}"/>

            <ComboBox x:Name="FilterComboBox" 
                     Width="150" 
                     ItemsSource="{Binding FilterOptions}"
                     DisplayMemberPath="Key"
                     SelectedValuePath="Value"
                     SelectedValue="{Binding SelectedFilterOption}"
                     Margin="8,0,8,0"/>

            <ui:Button Content="Refresh" 
                      Command="{Binding RefreshCommand}" 
                      Icon="ArrowClockwise24" 
                      Margin="0,0,8,0"/>

            <ui:Button Content="Clear History" 
                      Command="{Binding ClearHistoryCommand}" 
                      Icon="Delete24" 
                      Appearance="Danger"/>
        </StackPanel>

        <TextBlock Grid.Row="3" Margin="16,8,16,0" Text="{Binding Error, Mode=OneWay}" TextWrapping="Wrap">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding LoadState, Mode=OneWay}" Value="{x:Static enums:GenericTriState.Failed}">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                    <Setter Property="Visibility" Value="Collapsed" />
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <Border Grid.Row="3">
            <Border.Style>
                <Style TargetType="Border">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding LoadState, Mode=OneWay}" Value="{x:Static enums:GenericTriState.Unknown}">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                    </Style.Triggers>
                    <Setter Property="Visibility" Value="Collapsed" />
                </Style>
            </Border.Style>
            <ui:ProgressRing IsIndeterminate="True" />
        </Border>

        <ListView Grid.Row="3" ItemsSource="{Binding DisplayedHistory, Mode=OneWay}" Margin="8">
            <ListView.Style>
                <Style TargetType="ListView" BasedOn="{StaticResource {x:Type ListView}}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding LoadState, Mode=OneWay}" Value="{x:Static enums:GenericTriState.Successful}">
                            <Setter Property="Visibility" Value="Visible" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding HasHistory, Mode=OneWay}" Value="False">
                            <Setter Property="Visibility" Value="Collapsed" />
                        </DataTrigger>
                    </Style.Triggers>
                    <Setter Property="Visibility" Value="Collapsed" />
                </Style>
            </ListView.Style>
            <ListView.ItemContainerStyle>
                <Style TargetType="{x:Type ListViewItem}" BasedOn="{StaticResource {x:Type ListViewItem}}">
                    <Setter Property="Focusable" Value="False" />
                    <Setter Property="Padding" Value="4" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ui:Card Padding="12" Margin="0,4">
                        <Grid VerticalAlignment="Center">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Width="80" Height="80" CornerRadius="4">
                                <Border.Background>
                                    <ImageBrush ImageSource="{Binding UniverseDetails.Thumbnail.ImageUrl, IsAsync=True}" 
                               Stretch="UniformToFill"/>
                                </Border.Background>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding UniverseDetails.Thumbnail.ImageUrl}" Value="{x:Null}">
                                                <Setter Property="Background" Value="{DynamicResource ControlElevationBorderBrush}" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>

                            <StackPanel Grid.Column="1" Margin="16,0,16,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding UniverseDetails.Data.Name, FallbackValue='Unknown Game'}" 
                          FontSize="16" 
                          FontWeight="Medium" 
                          TextTrimming="CharacterEllipsis"/>

                                <!-- Server Type -->
                                <TextBlock Text="{Binding ServerTypeDisplay}" 
                          FontSize="11"
                          Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"
                          Margin="0,2,0,0"/>

                                <!-- Server ID -->
                                <TextBlock Text="{Binding ServerIdDisplay}" 
                          FontSize="11"
                                           TextWrapping="Wrap"
                          Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                          Margin="0,1,0,0"/>

                                <TextBlock Text="{Binding GameHistoryDescription}" 
                          FontSize="12"
                          Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                          TextWrapping="Wrap"
                          Margin="0,4,0,0"/>
                            </StackPanel>

                            <!-- Action Buttons -->
                            <StackPanel Grid.Column="2" VerticalAlignment="Center" Orientation="Horizontal">
                                <ui:Button Content="Rejoin" 
                          Command="{Binding RejoinServerCommand}" 
                          Appearance="Success"  
                          Icon="Play28" 
                          IconFilled="True"
                          Margin="0,0,4,0"
                          Padding="12,4"/>

                                <ui:Button
                          Command="{Binding CopyJoinLinkCommand}" 
                          Appearance="Secondary"
                          Icon="Copy24"
                          Padding="12,4"/>
                            </StackPanel>
                        </Grid>
                    </ui:Card>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Empty State -->
        <StackPanel Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center" Visibility="{Binding HasHistory, Converter={StaticResource InverseBooleanToVisibilityConverter}}">
            <ui:SymbolIcon Symbol="History24" Width="64" Height="64" Margin="0,0,0,16" 
                          Foreground="{DynamicResource TextFillColorTertiaryBrush}"/>
            <TextBlock Text="{x:Static resources:Strings.ContextMenu_GameHistory_NoHistory}" 
                      FontSize="16" 
                      Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                      TextAlignment="Center"/>
        </StackPanel>

        <!-- Status Bar -->
        <StatusBar Grid.Row="4" Background="Transparent">
            <StatusBarItem>
                <TextBlock Text="{Binding HistoryStats}" Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</base:WpfUiWindow>