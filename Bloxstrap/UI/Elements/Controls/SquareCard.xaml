<UserControl x:Class="Bloxstrap.UI.Elements.Controls.SquareCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             Height="225"
             SnapsToDevicePixels="True"
             x:Name="Control">

    <ui:Card SnapsToDevicePixels="True" Padding="14" Margin="8">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center">
                <ItemsControl ItemsSource="{Binding CategoryIcons, ElementName=Control}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,0,5,0">
                                <Border Padding="6" CornerRadius="8"
                                        Background="{Binding BorderColor}" Opacity="0.15"/>
                                <Border Padding="6" CornerRadius="8" Background="Transparent"
                                        ToolTip="{Binding ToolTip}">
                                    <Image Width="24" Height="24" Source="{Binding Icon}"/>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                    <ItemsControl.Style>
                        <Style TargetType="ItemsControl">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Categories, ElementName=Control}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Categories, ElementName=Control}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ItemsControl.Style>
                </ItemsControl>
            </StackPanel>

            <ContentPresenter Grid.Row="0" Grid.Column="1" 
                              Content="{Binding InnerContent, ElementName=Control}" 
                              VerticalAlignment="Center" 
                              HorizontalAlignment="Right" 
                              MaxWidth="96"/>

            <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Height="200" Margin="0,6,0,0">
                <TextBlock FontSize="18" FontWeight="Medium" TextWrapping="Wrap" Text="{Binding Title, ElementName=Control}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Title, ElementName=Control}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <TextBlock FontSize="14" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" Margin="0,12,0,0"  Foreground="{DynamicResource TextFillColorTertiaryBrush}" Text="{Binding Description, ElementName=Control}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Description, ElementName=Control}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Grid>
    </ui:Card>
</UserControl>