namespace Froststrap.Enums.BootstrapperDialogs
{
    public enum TaskDialogProgressBarState
    {
        Normal = 0,
        Paused = 1,
        Error = 2,
        Marquee = 3,
        MarqueePaused = 4,
        None = 5
    }
}
